// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Itinerary {
  name        String
  description String?
  date        String 
  events      Event[]
  attendees   Attendee[]
  @@id([name, date])
  @@unique([name, date])

  createdAt DateTime
  createdBy String
  updatedAt DateTime
  updatedBy String 
  deletedAt DateTime?
  deletedBy String?
}

model Event {
  name          String
  starts        String
  ends          String
  link          String
  itinerary     Itinerary  @relation(fields: [itineraryName, itineraryDate], references: [name, date])
  itineraryName String
  itineraryDate String
  @@unique([name, itineraryName, itineraryDate])
  @@id([name, itineraryName, itineraryDate])

  createdAt DateTime
  createdBy String
  updatedAt DateTime
  updatedBy String 
  deletedAt DateTime?
  deletedBy String?
}

model Attendee {
    name          String
    email         String
    itinerary     Itinerary  @relation(fields: [itineraryName, itineraryDate], references: [name, date])
    itineraryName String
    itineraryDate String
    @@unique([itineraryName, itineraryDate, email])
    @@id([itineraryName, itineraryDate, email])

    createdAt     DateTime
    createdBy     String
    updatedAt     DateTime
    updatedBy     String 
    deletedAt     DateTime?
    deletedBy     String?
} 
