import clsx from 'clsx';

export const Footer = (): JSX.Element => {
    return (
        <div className={clsx('flex w-full h-16 items-center justify-center border')}>
            <div>FOOTER</div>
        </div>
    )
}